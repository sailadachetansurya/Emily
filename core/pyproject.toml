[project]
name = "emotive-pipeline-baseline"
version = "0.1.0"
description = "Baseline modular emotive AI pipeline"
requires-python = ">=3.10"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
